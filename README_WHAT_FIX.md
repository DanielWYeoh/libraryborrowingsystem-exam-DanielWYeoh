This is what i've fixed in the code

## Bug Fixes

* Adding the missing `getGenre()` method in `Book.java` — line 118.
* Fixed the overridden `getInfo()` method in `Multimedia.java` — lines 54–55.
* Corrected the `author` and `genre` argument order in `BookFXController.java` — line 193.
* Fixed the mistypo `getValue().author` into `getValue().getAuthor` in `BookFXController.java` - line 106.
* Fixed the Reservation menu calling the Borrow menu in `LibraryController.java` — line 64.
* Fixed the MVC structure in `BorrowView.java` — line 33.
* Updated `BorrowController.java` to pass borrow records to the View — lines 46 and 134.